# 轻练 Registry

> 当前快照，可整体替换。历史依据见 `decision-log.md` 与 GitHub issue/PR。截至 2026-09-14。

## 现在在哪里

**开发执行权**：2026-09-14 起由 Claude 负责。开发事实源为 GitHub `zinan92/get-fit`（issue 即合同，一 issue 一 PR）。

**方向（已批准）**：首客走「个人主体小程序体验版 + 微信云开发」，不等执照、不做备案。合同：`docs/specs/2026-09-14-first-customer-shortest-path.md`（Park 2026-09-14「全按推荐，开工」），里程碑标签 `pilot-path`。

**网页演示**：https://fit-plan-mockup.parkzz.chatgpt.site（ChatGPT Sites 私有托管，owner-only；保留为演示与设计沙盒，不在试点数据链路上；推送需 Codex 中转）。

**已具备的能力**

- 单教练闭环 API：邀请 → 登录 → 同意/建档 → 计划生成交接 → 草案编辑 → 发布 → 版本化；打卡、疼痛告警、提醒授权、30 天删除清扫。
- 计划校验器：只允许目录内动作/食物 ID，拒绝未知字段，kcal 由服务端按目录与克数重算。
- 同一份 API 源码可打包为云函数（`npm run build:cloudfunction`，#9）。云函数模式下身份只认微信 OPENID，教练走 `COACH_OPENIDS` 白名单，状态存云开发文档并按修订号比较写入，冲突自动重试；打开天数独立记录（#10，均为 fake 数据库下的测试证据）。
- 动作库 49 个动作 / 19 种动作模式、食物库 60 种（热量有 USDA / 中国食物成分表来源），每个条目都有 v3 角色（#16）；校验器按客户器械屏蔽做不了的动作。
- Web 客户三页为 v3 视觉；设计源 `design/fit-plan-v3.html`。
- 小程序「今天」「计划」页已是 v3 视觉（#12）：分层动画角色、Fredoka 数字、歪框打卡弹正、食物 kcal 与餐次/当日合计；数据全部走 `api` 云函数。DevTools 游客模式下用真实 API 生成的预览数据，截图见 `docs/evidence/miniprogram/2026-09-14/`。邀请/分项同意/建档/身体反馈也是 v3（#13），选项与风险分级共用一份词表。教练模式（#14）：工作台、确认资料、发起计划、逐天审阅（与客户所见同一组件）、发布/打回、7 天数据、身体提醒。

**验证**：`npm test` 20 渲染/云函数打包/小程序契约/预检 + 29 API/持久化/云函数/目录全过，`tsc`、`lint`、`gitleaks` 通过。

**状态**：代码闭环 `verified`；云函数真实运行时（Nodejs20.19）`unknown`，待 #11 健康检查；微信身份 / 云开发读写 / 首位客户验收 `partial/unknown`；上线合规评估 `blocked`（不挡体验版试点）。

## 下一步

| 顺序 | Issue | 内容 | 状态 |
|---|---|---|---|
| 并行 | #8 S0 | Park 注册个人主体小程序、开云开发、加体验成员、本机工具登录 | 等 Park |
| 1 | #9 S1a | 云函数打包与运行时 spike | 已合并 |
| 2 | #10 S1b | 云开发存储、OPENID 身份、教练白名单、版本冲突、打开天数 | 已合并 |
| 3 | #12 S2a / #16 S4b | 小程序今天/日历/打卡 v3；动作与食物库 | 已合并 |
| 4 | #13 S2b、#14 S3 | 邀请/同意/建档/反馈；小程序内教练模式 | 已合并 |
| 5 | #11 S1c、#15 S4a | 部署与健康检查（工具已就绪，等 #8）；操作员起草命令 | #15 已合并；#11 等 #8 |
| 门 | S5 | 上传体验版 → Park 与教练真机 → 首客七天 | 最后 |

**挡第二个客户的已知缺口**：起草依赖操作员本机 Codex；单快照存储（已防覆盖、只读不写回，但仍整份读写）；生成时去标识档案发往境外模型需合规评估。

**首客试用成功标准**：打开 ≥ 5 天、打卡 ≥ 3 次、教练资料确认到发布 ≤ 15 分钟、所有疼痛告警有人工处理。
