# 轻练 Registry

> 当前快照，可整体替换。历史依据见 `decision-log.md` 与 GitHub issue/PR。截至 2026-09-14。

## 现在在哪里

**开发执行权**：2026-09-14 起由 Claude 负责。开发事实源为 GitHub `zinan92/get-fit`（issue 即合同，一 issue 一 PR）。

**方向（已批准）**：首客走「个人主体小程序体验版 + 微信云开发」，不等执照、不做备案。合同：`docs/specs/2026-09-14-first-customer-shortest-path.md`（Park 2026-09-14「全按推荐，开工」），里程碑标签 `pilot-path`。

**网页演示**：https://fit-plan-mockup.parkzz.chatgpt.site（ChatGPT Sites 私有托管，owner-only；保留为演示与设计沙盒，不在试点数据链路上；推送需 Codex 中转）。

**已具备的能力**

- 单教练闭环 API：邀请 → 登录 → 同意/建档 → 计划生成交接 → 草案编辑 → 发布 → 版本化；打卡、疼痛告警、提醒授权、30 天删除清扫。
- 计划校验器：只允许目录内动作/食物 ID，拒绝未知字段，kcal 由服务端按目录与克数重算。
- 同一份 API 源码可打包为云函数（`npm run build:cloudfunction`，#9），在纯 Node 下跑通邀请到打卡主流程，云函数入口强制忽略 `DEV_MODE`。
- Web 客户三页为 v3 视觉；设计源 `design/fit-plan-v3.html`。
- 原生小程序页面骨架（`apps/miniprogram/`），尚未采用 v3 视觉、仍用 `wx.request`，未部署。

**验证**：`npm test` 13 渲染/云函数打包 + 21 API 全过，`tsc`、`lint`、`gitleaks` 通过。

**状态**：代码闭环 `verified`；云函数真实运行时（Nodejs20.19）`unknown`，待 #11 健康检查；微信身份 / 云开发读写 / 首位客户验收 `partial/unknown`；上线合规评估 `blocked`（不挡体验版试点）。

## 下一步

| 顺序 | Issue | 内容 | 状态 |
|---|---|---|---|
| 并行 | #8 S0 | Park 注册个人主体小程序、开云开发、加体验成员、本机工具登录 | 等 Park |
| 1 | #9 S1a | 云函数打包与运行时 spike | 本 PR |
| 2 | #10 S1b | 云开发存储、OPENID 身份、教练白名单、版本冲突、打开天数 | 下一个 |
| 3 | #12 S2a / #16 S4b | 小程序今天/日历/打卡 v3；首批动作食物目录（需教练清单） | 待开 |
| 4 | #13 S2b、#14 S3 | 邀请/同意/建档/反馈；教练模式 | 待开 |
| 5 | #11 S1c、#15 S4a | 部署与健康检查；操作员生成命令 | 依赖 #8 |
| 门 | S5 | 上传体验版 → Park 与教练真机 → 首客七天 | 最后 |

**挡第二个客户的已知缺口**：`hasManualRisk` 不分轻重；生成依赖本机 CLI；单快照存储；800 kcal 固定下限与无训练频率校验；生成时去标识档案发往境外模型需合规评估。

**首客试用成功标准**：打开 ≥ 5 天、打卡 ≥ 3 次、教练资料确认到发布 ≤ 15 分钟、所有疼痛告警有人工处理。
