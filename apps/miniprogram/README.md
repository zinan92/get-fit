# 轻练微信小程序客户端

这是原生微信小程序客户端的 V1 代码表面。它只调用 `/api`，不保存 DeepSeek key，也不接触教练后台接口。

开发者工具中把 `apiBaseUrl` 配成 Worker 地址；本地联调时可使用 `http://localhost:3000` 并在 Worker 的 `DEV_MODE=true` 下使用开发登录。

进入流程：邀请 → 微信登录 → 两类同意 → 建档 → 等待教练发布 → 今日计划 → 打卡/反馈 → 删除申请。
