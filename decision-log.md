# Decision Log

## 2026-08-11 · 首版客户端 mockup

- 首版只解决客户每天打开后知道练什么、吃什么、注意什么。
- 计划生命周期为 AI 生成 30 天草案、教练一次性审核修改、客户仅看到已确认内容。
- 示例客户为每周训练三次的减脂新手。
- 饮食展示到具体食物、可食用克数与单项 kcal，并配原创可爱插画。
- 打卡仅记录完成或未完成，不要求图片或文字。

## 2026-09-14 · v3 视觉方向、开发移交与 dev 身份边界

- 产品的核心价值是情绪价值（Park）：要可爱、smooth、从头到尾一致。此前版本被评为"工具人版本"。
- 视觉统一为一套手绘角色语言：食物与动作演示都是带脸、带腮红的角色，脸画在主体上；动作演示用循环动画。页面不使用任何 emoji 或 Unicode 装饰字符。
- 打卡框静止时歪斜，点击后弹正、填色、勾线画出——"失序到归位"是打卡的情绪反馈。
- 颜色只承担分类语义：训练橙 `#D85C28`、饮食绿 `#4F9D6E`；数字用 Fredoka，中文保持系统字体。
- 设计源 `design/fit-plan-v3.html` 是唯一真相；实现以截图对照设计源验收，不以测试通过代替。
- 开发执行由 Codex 移交 Claude；GitHub `zinan92/get-fit` 为开发事实源。
- dev 身份只认服务端 `DEV_MODE`，不认请求 Host：Host 由客户端控制，挂在它上面的鉴权在客户 API 公开后即成旁路。

## 2026-09-14 · 首客最短路径：云开发 + 个人主体体验版

- Park 批准 `docs/specs/2026-09-14-first-customer-shortest-path.md` 全部推荐项（D1–D4）。
- 后端迁到微信云开发；领域逻辑与 `plan-schema` 抽成共享代码，Sites 网页和云函数两个入口消费同一份，只有存储与身份适配器不同。
- 新注册个人主体小程序「轻练」，首客在体验版试点；不等个体户执照、不做小程序备案（两者只卡正式上架）。
- 试点期生成由操作员在 Mac mini 上运行（ADR 0002 修订）；教练在小程序内教练模式审阅和发布（ADR 0003 修订）。
- 更正：此前"域名备案 + 主体资质是首客前置门"的判断是错的，已在规格中留痕。

## 2026-09-14 · 云函数以打包产物承载同一份 API 源码（#9）

- 云函数产物由 rolldown 从 `server/cloudfunction/entry.ts` 打包，领域逻辑与 Worker 共用 `server/api`、`packages/*`；不在 `cloudfunctions/` 下手写第二份。
- 入口把 `callFunction` 事件适配为 `Request` 交给 `handleApi`，返回前等待所有 `waitUntil` 任务——云函数实例返回后可能被冻结。
- 入口在构造环境时强制 `DEV_MODE: undefined`，与 Worker 的 dev 身份边界一致且更严：云函数永远不是本地沙盒。
- 语法降级目标写死为 CloudBase 运行时 `node20.19`，不跟随本机 Node 版本。

## 2026-09-14 · 云函数身份与存储：平台 OPENID、教练白名单、比较写入（#10）

- `ApiContext.platform` 存在时是唯一身份来源：客户由 OPENID 哈希映射到 clientId，教练由环境变量 `COACH_OPENIDS` 白名单判定；Bearer 会话、`x-coach-token`、Sites 边缘头、请求体里的 openid/clientId 一律不参与。Worker 不传 `platform`，行为不变。
- 云函数入口只转发 `idempotency-key` 头；传给 handlers 的环境只含加密密钥和提醒模板 ID，拿不到 DEV_MODE、COACH_TOKEN、AppSecret。
- 快照存储抽象为 `SnapshotBackend`（读 + 按修订号比较写入）。云开发实现：首次写用固定 `_id` 插入（并发插入报重复即冲突），之后 `where({_id, revision}).update` 只在修订号未变时生效。
- 冲突时整次请求在重新读取的状态上重跑（默认最多 3 次），仍失败返回 409 `STATE_CONFLICT`；从不在旧状态上覆盖别人的写入。
- 「打开天数」改为一等数据 `openedDays`（每客户每本地日期一条），教练摘要和日历只读它，不再依赖只保留 500 条的审计日志。
- D1 路径仍是无条件覆盖写，只服务 Sites 演示，不在试点数据链路上；多客户前再统一。

## 2026-09-14 · 小程序今天/计划页落地 v3（#12）

- 角色插画的唯一源改为 `packages/illustrations`：网页把分层合成为一个 SVG（与原字符串逐字节一致），小程序每层一张图片叠放，动的层用 WXSS 循环，支点按网页 `fill-box` 分组换算成整框百分比。
- 小程序资源（角色、图标、数字字体、查表模块、预览数据）由 `scripts/build-miniprogram-assets.ts` 生成并提交；`npm test` 检查漂移。
- 数字字体用 Fredoka SemiBold 的数字子集（约 4 KB，SIL OFL），以 base64 写进 WXSS，不依赖网络域名。
- 预览数据由真实 API 代码跑一份演示计划生成，只在没有真实 AppID 时启用（DevTools 游客模式 AppID 为空或 `touristappid`），页面显示「本地预览数据」。
- 「打开天数」改记请求当天的真实日期，不再记客户翻看的计划日期——日期条和日历翻看未来日期会虚增七天验收指标。
- 与设计源的有意偏差：计划数据没有餐次时间和热身安排，因此不显示（不编造）；恢复日没有动作时显示恢复日卡片。

## 2026-09-14 · 动作库与食物库扩到可排 30 天（#16）

- Park：教练清单不存在，动作库和食物库由开发方自己维护。
- 动作 49 个（此前 3 个真动作 + 快走），覆盖蹲、弓步、髋铰链、臀桥、划船、俯卧撑、卧推、推举、平举、弯举、撑体、卷腹、死虫、平板、登山跑、开合跳、提踵、拉伸、快走 19 种动作模式；每个带初/中级、所需器械、禁忌标签、单位（次/秒/分钟）和来源。
- 动作文字来自 hasaneyldrm/exercises-dataset 的数据部分（MIT），中文步骤逐条校订；该仓库的动图版权属 Gym visual，不使用。
- 食物 60 种，六大类各至少 4 种；热量以 USDA FoodData Central SR Legacy 为主（公共领域，按 fdcId 回查）。原有食物的热量随来源统一而更新（如水煮蛋 143 → 155 kcal/100g）。
- 角色按动作模式画：新增 15 个分层动作角色、48 个食物角色，全部由共享部件生成，保持脸画在身体上、腮红、训练橙躯干；动作循环改为数据驱动（静止 ↔ 峰值 + 关节支点），小程序动画样式由生成器产出。
- 校验器按客户声明的器械屏蔽动作（`gym` 视为全器械；有哑铃不代表有平凳），避免计划里出现客户做不了的动作。
- 教练日历里「只有快走/拉伸」的日子算恢复日。
- 生成提示词只带排计划需要的字段（不带步骤正文和来源），控制提示词体积。

## 2026-09-14 · 风险分级（#22）

- 轻微不适（膝/腰/肩/腕/踝）和 8 类常见过敏原由系统自动处理：确认资料仍可自动生成，校验器屏蔽带对应禁忌标签的动作和含过敏原的食物。
- 任何 `riskFlags`（未成年、孕期、急性疼痛、慢病、饮食障碍等）、急性疼痛类伤病标记、未识别的标记一律转教练人工。
- 词表只在 `packages/contracts` 定义一处，建档页与服务端共用。

## 2026-09-14 · 邀请、同意、建档与身体反馈（#13）

- 建档页的所有选项（目标、年龄段、器械、轻微不适、过敏原、需人工的情况）和中文文案只在 `packages/contracts` 定义，生成器产出小程序选项模块；风险分级与建档页不会各说各话。
- 年龄新增「未满 18 岁」，选中即转教练人工。
- 服务端要求先有两项必需同意才能保存资料（此前只在教练确认时检查）。
- 邀请口令可由分享链接 `?invite=` 或小程序码 `scene` 自动带入；进入后按 `/api/me` 恢复到上次停下的步骤。
- 身体反馈放在今天页训练之后：有疼痛时提示先停下、不要自己换动作，教练收到告警；反馈从不修改计划。
- 同意页如实说明「去掉身份后交给第三方模型起草、教练确认后才可见」；客户每天使用的页面仍不出现生成相关字样。

## 2026-09-14 · 小程序内教练模式（#14）

- 教练入口在「我的」页，只有教练白名单里的微信能看到；只做教练、不是客户的微信打开小程序时直接进入工作台，不会被送进建档。
- 工作台一屏看完：待处理的身体提醒、每位客户所处阶段（资料待确认 / 需要先沟通 / 可以安排计划 / 计划准备中 / 计划待审阅 / 执行中）、邀请新客户（分享卡片直接带口令进入建档）。
- 新接口 `GET /api/coach/overview` 一次返回所有客户阶段；`GET /api/coach/plan-drafts/:id/preview` 返回与客户今天页完全相同的投影（有测试断言发布前预览与发布后客户所见逐字段一致）。
- 今天页的计划展示抽成 `plan-day` 组件，客户今天页与教练逐天审阅共用；审阅时只读，徽标由调用方传入，组件本身不含草案字样。
- 教练可以确认资料、选开始日期发起计划准备、逐天审阅后发布或带理由打回、查看执行中客户最近 7 天的打开/打卡/疼痛提醒、把提醒标记为已沟通。
- 试点期不提供教练端结构化改动作；要改就打回并写明原因，由操作员重新准备。

## 2026-09-14 · 操作员起草、计划质量提醒与部署工具（#15、#11 准备）

- 操作员（Wendy，在 Mac mini 上）用 `npm run operator -- list / run <jobId>` 起草：取去身份档案 → 本机 Codex 起草 → 本地同一校验器 → 一次性令牌导入。校验不过的草案不上传。
- 操作员授权：云函数只存操作员密钥的 SHA-256（`OPERATOR_KEY_SHA256`）；带 OPENID 的调用永远不是操作员；操作员只能列出待起草任务、读取去身份档案、领导入令牌，不能做任何教练或客户操作。偏离 #15 评论里「教练在小程序里签发令牌」的设想：每次都要教练点一下，对教练是额外负担，而窄权限密钥已经把风险限制在「能起草、不能发布」。
- 操作员重试会替换未用的导入令牌（教练路径仍然拒绝重复签发）。
- 起草提示词只列出这个客户能用的动作和食物（禁忌、过敏、器械、新手级别都先过滤），给出每周训练次数、单次时长、按体重和目标估算的每日热量范围、餐次和轮换规则。
- 计划质量提醒（不阻塞，随草案给教练）：每周训练日与客户可练次数相差超过 1、每日热量不在参考范围（按体重×目标系数，下限 1200）、预计训练时长超过单次时长 25%、新手计划里出现进阶动作。这取代了「固定 800 kcal 下限、不查频率」的缺口。
- 部署工具 `node scripts/cloudbase-ops.mjs init / add-coach / deploy / health`：密钥只在 `~/.config/qinglian`（600）；存储密钥只生成一次，拒绝覆盖。云函数新增健康检查（运行时能力、存储可用该密钥解密、配置是否齐全，只报布尔和数量）。
- 教练名单改为可用「账号编号」（OPENID 的 SHA-256）配置：教练在「我的」复制编号发给操作员，谁都不需要拿到原始 OPENID。

## 2026-09-14 · 云函数只在状态变化时写回；删除到期定时清理（#27）

- 每个请求在处理前后各算一次「持久状态指纹」（快照去掉浏览类审计），相同就不写存储：翻看计划、日历、总览不再整份重写，也就不会和教练发布抢修订号。每天第一次打开仍会写（记录打开天数）。
- 删除到期清理抽成 `server/api/retention.ts`，Worker 与云函数共用。
- 云函数定时触发器 `daily-retention` 每天 03:00 运行；只接受无 OPENID 的调用（定时器/运维）；停止开关 `RETENTION_JOB_ENABLED=false`；健康检查报告开关状态。按手册，这是产品运行时任务，有独立契约、证据和停止方法（写在 runbook）。

## 2026-09-14 · 隐私保护指引与小程序内隐私说明（#28）

- `docs/privacy/user-privacy-guide.md`：后台「用户隐私保护指引」要勾选的接口（目前只有剪切板）和完整隐私说明草稿，写明境外模型只收到去身份资料、数据存境内云开发、30 天冷静期删除。需 Park 审阅，不是法律意见。
- 小程序新增隐私说明页，同意页和「我的」可进入；测试保证代码里用到的隐私接口都已在指引中声明。

## Gotchas

- 热量必须绑定份量；只有食物名称的热量数字没有可信含义。
- 未经教练确认的 AI 草案不能进入客户端。
- 本 mockup 的训练和饮食数字不可直接用于真实客户；后续实现需要独立的健康信息、过敏、伤病和风险边界设计。
- `.openai/hosting.json` 是构建输入（`vite.config.ts` 静态 import、Sites 插件拷进部署产物），不能 gitignore；其内容只是项目标识与 binding 名，且早已在公开历史中，忽略它不换来任何隐私，只会让干净 clone 无法构建。验证构建时必须用全新 clone，不能复制工作树——被忽略的本地文件会让验证假绿。
- 设计稿里的 `<div>` 移植进 `<button>` 时会被改成 `<span>`（button 内只允许短语内容），span 是行内元素会横排；必须给容器补纵向布局（参照 `.calcell` 的 `display:flex; flex-direction:column`）。渲染测试只断言文本和类名，测不到这类布局回归，视觉移植必须截图对照设计源。
- 生产环境永远不能设置 `DEV_MODE`；dev 身份的全部防线都压在这一个环境变量上。
- `/sandbox` 页面里的 `window.location.hostname` 判断只是浏览器端提示，不是安全边界；真正的门在服务端 `DEV_MODE` 与邀请绑定校验。
- 小程序备案与域名 ICP 备案是两道不同的门：前者只卡上架，不卡体验版；后者卡所有 `wx.request` 服务器域名，体验版真机同样校验（仅手机开调试模式时跳过，不能拿来给真实客户用）。云开发 `callFunction` 两道都不经过。
- 打包产物目录 `dist-cloudfunctions/` 必须同时被 `.gitignore` 和 ESLint 忽略，否则 lint 会检查生成代码并报错。
- 本机 Homebrew `node@22` 缺 simdjson 动态库无法启动；运行时兼容性不能靠本机多版本验证，要以部署后健康检查回报为准。
- 云函数冲突重试会重跑整个 handler：handler 里对外的副作用（发消息、调外部 API）必须能容忍「执行了但没落库」，或放到落库成功之后。
- 平台模式下没有 OPENID 的调用（例如本机 CLI 直接 invoke 云函数）什么身份都不是；操作员生成流程（#15）需要单独设计授权，不能靠放宽教练判定。
- DevTools 游客模式下 `wx.getAccountInfoSync().miniProgram.appId` 是空字符串，不是 `touristappid`；两者都要当作预览模式。
- `miniprogram-automator` 0.12 在稳定版 DevTools 上 `Tool.getInfo` 不返回 SDKVersion，`connect` 会崩；需像 xingqiu 一样改写 `checkVersion` 读 `systemInfo()`。
- `<image>` 里的 SVG 不继承 CSS 颜色，`currentColor` 图标必须在生成时替换成具体色值；动画分组也必须拆层，SVG 内部的 class 动画不生效。
- 外部动作数据集的中文是机翻：直接用会出现“挤压二头肌”“英寸”“重复所需的重复次数”这类句子，测试会拦下未校订的句子。
- USDA 在线接口的 DEMO_KEY 每小时只有约 30 次；批量取数用 SR Legacy 离线包（nutrient 1008 = kcal）。
- 小程序自定义组件默认拿不到 app.wxss 的类；`plan-day` 用 `styleIsolation: 'apply-shared'` 共用打卡框、卡片等全局样式。
- WXML 的 `wx:key` 只能写 item 的直接属性名，不能写 `client.id` 这类路径。
- 存储密钥（`DATA_ENCRYPTION_KEY`）换了旧数据就解不开；init 拒绝覆盖，健康检查会把「密钥不匹配」报成 `storage: unreadable`。
- 本地 Codex 一次写满 30 天 JSON 需要几分钟，180 秒上限会超时；上限改为默认 10 分钟，可用 `CODEX_TIMEOUT_MS` 调整。
- CloudBase 定时触发器传给函数的是 `{ Type: "Timer", TriggerName }`，不是业务事件；入口要按触发器名映射到清理动作。
- 小程序调用剪切板等隐私接口前，必须在后台「用户隐私保护指引」里声明，否则接口直接失败；新增接口要同步改指引。

## 2026-09-14 · 食物库只用 USDA 来源（#32）

- 《中国食物成分表》查询平台声明未经许可不得商用，Park 决定换成 USDA 近似项；食物库 60 项现在全部可按 fdcId 回查。
- 馒头：USDA 没有馒头，按白面包（fdcId 174924，266 kcal/100g）计，条目 `approximation` 写明比中国表均值高约 18%。热量偏高方向是保守的：客户实际摄入不会多于计划显示。
- 小米粥换成燕麦粥（fdcId 173905，71 kcal/100g）。USDA 的 Millet, cooked 是 119 kcal 的小米饭，当粥用会高估约 2.6 倍，所以不拿它顶替。
- Gotcha：近似条目要在数据里留痕（`approximation`），否则以后没人知道「馒头」的热量其实是面包。

## 2026-09-14 · 教练逐项改计划草案（#35）

- 教练在草案逐天预览里「改这一天」：动作调组数/次数/休息、换、删、加；每餐调克数、换、删、加；改主题和提醒语。
- 可换的动作和食物由服务端 `GET /api/coach/plan-drafts/:id/options` 给出，复用校验器的禁忌、器械、过敏过滤；小程序里没有任何安全规则。
- 保存仍发整份计划到原有 PATCH，同一个校验器把关；不通过时返回 `details.messages`，把「days[2].exercises[0].catalogId: blocked…」翻成「第 3 天「高脚杯深蹲」不适合这位客户」。
- 换成不同计量方式的动作（次数 ↔ 秒 ↔ 分钟）时组数次数回到该类默认值；教练给旧动作写的要点随之清掉。
- Gotcha：自动化里在 `pageScrollTo` 之后立刻 tap 按钮不一定触发 `navigateTo`；截图脚本直接导航到编辑页。
- 预览模式下编辑只保存在本机内存，草案预览页仍显示生成时的数据。

## 2026-09-14 · 会员管理（#36）

- 工作台概览每位会员带：当前一期第几天/剩几天（`course`）、最近打开日期、需要注意的事（`attention`：几天没打开 / 还剩几天 / 这一期已结束）、教练备注、是否归档。
- 「几天没打开」只在这一期已经开始后才算；两天及以上才提醒。「快结课」是最后 5 天，已经排好下一期就不再提醒。
- 备注和归档存在 ClientRecord 上（`coachNote`、`archivedAt`），随加密快照持久化；`clientView` 不带这两个字段，客户接口永远拿不到备注。
- 归档只影响教练列表，不改客户状态、计划和记录。
- 续期复用原有「开始准备计划」：开始日期默认为本期结束次日（不早于今天），发布时自动接在上一期后面生效。
- 服务端所有时间都走 `Date.now`（`nowIso` 也改成读它）；预览数据生成时固定时钟，数据集不再随真实日期漂移。
- Gotcha：WXML 里 `wx:elif` 接的是紧挨着的上一个兄弟节点，中间插入带 `wx:if` 的元素会改变条件链。

## 2026-09-14 · 每周小结卡与教练寄语（#37）

- `GET /api/me/week`：北京时间今天所在的周一到周日；每天是训练日/恢复日/无计划、是否练了、是否有打卡；本周训练完成数/计划数；连续打卡天数（今天还没打卡时算到昨天）；教练寄语。窗口锚定今天，不锚定最后有数据的日期。
- 今天页（只在看今天时）显示「这一周」：练过的日子是弹正的橙色勾，没到的训练日是虚线框，恢复日是绿点；连续 2 天以上才显示连续天数；有寄语时显示「教练说」气泡。
- 文案只鼓励不追责：不出现「没练」「落后」之类，合同测试拦截。
- 教练在会员详情写「给 TA 的一句话」（≤ 60 字，清空即撤回），存 ClientRecord `coachMessage`，随快照持久化。
- 周卡片请求失败不影响当天计划显示；打卡后刷新周卡片。

## 2026-09-14 · 调整已发布的计划（#41）

- 会员详情「调整计划」：选生效日（最早明天、最晚本期最后一天）→ `POST /api/coach/clients/:id/plan-revisions` 把覆盖该日的已发布计划复制成待审阅草案（`revision { planId, effectiveFrom }`）→ 用 #35 的编辑器改 → 预览只列生效日及以后 → 确认调整或放弃。
- 发布调整：新版本 `effectiveFrom` = 生效日，`effectiveTo` 沿用被调整版本的结束（已排好下一期时就是下一期开始日），版本号取最大 +1。生效日之前的日子继续读原版本，打卡记录的 `planDayId` 不受影响。
- 服务端拦截改动生效日之前的日子（422，中文原因），同一会员同时只允许一份待审阅草案（409 `DRAFT_PENDING`）；生效日在发布时已过则拒绝，重新发起。
- 调整稿不经过操作员起草，不出现在 `operator/jobs` 里。
- Gotcha：发布普通草案会把「最新版本」标成 superseded 并截断；调整稿必须找「覆盖生效日的版本」，否则在已排好下一期时会把下一期截掉。

## 2026-09-15 · 教练「最近 7 天」以今天为终点

- `coachSummary` 的 7 天窗口原来以「最后一天有数据」为终点：客户第 5–7 天不打开，面板仍像满满一周，恰好掩盖试点要发现的问题。改为以北京时间今天为终点，响应带 `endDate`。
- 试点第 7 天验收（打开 ≥ 5 天）按这个面板读。
- 调整稿发布时生效日已过（晚上发起、第二天才确认），预览页提示「调整日期已经过了，放弃后重新发起」，不再只显示「没发布成功」。
